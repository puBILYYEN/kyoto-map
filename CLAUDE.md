# 給 AI 的入口

接手這個專案前，**請先完整讀過 [`HANDOFF.md`](HANDOFF.md)**：
架構、部署流程、資料格式、除錯方法（含系統記錄 .log 怎麼看）、以前出過的 bug 都在裡面。

最重要的三條：
1. 使用者回報故障時，先請他按網站上的「🪵 下載系統記錄（.log）」把檔案傳給你，看完記錄再判斷原因。
2. 每次改前端檔案都要把 `sw.js` 的 `VERSION` 加 1，並在 `VERSION.txt` 最上面加一筆紀錄。
3. `renderMarkers()` 很早就同步執行，它用到的 `const`/`let` 必須宣告在 `app.js` 最上面（否則地圖標記全部消失）。

與使用者溝通一律用繁體中文。
