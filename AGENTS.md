# 給 AI 的入口

接手這個專案前，**請先完整讀過 [`HANDOFF.md`](HANDOFF.md)**：
架構、部署流程、資料格式、除錯方法（含系統記錄 .log 怎麼看）、以前出過的 bug 都在裡面。

**9B 等小模型（例如 Ornith-1.5-9B）請改讀短版 [`HANDOFF-9B.md`](HANDOFF-9B.md)**，
不要讀整個 `app.js` / `data.js`（塞不進上下文），常見任務用 `tools/` 裡的現成指令。

最重要的六條：
1. 使用者回報故障時，先請他按網站上的「🪵 下載系統記錄（.log）」把檔案傳給你，看完記錄再判斷原因。
2. **一次只改幾行，不要把整個檔案重新輸出**（app.js 兩千多行，整份重寫很容易被截斷）。
3. 改完一定跑 `node tools/check.js`，有 ❌ 不准 commit。
4. 每次改前端檔案都要把 `sw.js` 的 `VERSION` 加 1，並在 `VERSION.txt` 最上面加一筆紀錄。
5. 網站內容與跟使用者溝通一律**繁體中文、台灣用語**，不要出現簡體字。
6. `renderMarkers()` 很早就同步執行，它用到的 `const`/`let` 必須宣告在 `app.js` 最上面，否則地圖標記會全部消失（`tools/check.js` 抓不到這個，要自己注意）。
