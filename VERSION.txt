版本：v20
更新時間：2026-09-21

本版修正（/healthz 的 configured 判斷失真）：
- 原本只檢查環境變數「有沒有填東西」，填 placeholder 也會回報 configured:true
  → 實際上根本不能用，卻顯示設定完成，等於沒有檢查
- 改為確認端點是合法的 http(s) 網址、且金鑰不是明顯的佔位字串
- /healthz 一併回報目前使用的端點網址（只回報網址，金鑰絕不外流）
- 尚未正確設定時，/ask 會明確說「目前看起來還是佔位值」

v19（後端支援不同供應商的端點路徑）：
- 原本寫死 AI_BASE_URL + "/v1/chat/completions"，Gemini 的路徑是
  /v1beta/openai/chat/completions，會組出錯誤網址
- 新增選填的 AI_CHAT_URL，可直接指定完整端點網址
- /healthz 一併回報目前使用的模型名稱，方便確認設定有沒有生效
- ⚠️ 改動了 ai-backend/，因為 autoDeploy 是關閉的，
  需要到 Render 按一次 Manual Deploy 才會生效

v18（導遊後端接上線）：
- AI_CONFIG.endpoint 填入 https://kyoto-ai.onrender.com
- 後端已部署在 Render（Blueprint 管理，服務名稱 kyoto-ai）
- 注意：Render 的 AI_BASE_URL / AI_API_KEY 目前仍是 placeholder，
  所以導遊按下去會連到後端但還無法回答，要等填入真正的 AI 端點

v17（檢索命中率 83% → 100%）：
- 用 30 題實際會問的問題做基準測試，修掉 4 個找不到的情況：
  「金色的寺廟」→ 金閣寺、「坐船」→ 保津川遊船、「我們住哪」→ 住宿、
  「買藥妝」原本會撈到和服店（因為同屬「穿搭與藥妝」分類），改為指向實際賣藥妝的地點
- 單字查詢（例如只剩「住」）原本會被丟掉，現在也能查
- 同義詞表補上金色／銀色／坐船／爬山／姻緣／斷惡緣

v16（導遊的景點檢索 / RAG）：
- 導遊現在會依問題，從 114 個景點裡自動撈出最相關的幾筆一起送過去
  → 問「哪裡可以吃素」會撈出精進料理那幾家，即使一個都沒勾選
- 中文沒有空白可斷詞，改用「兩字一組」比對；名稱／地區／分類權重高於介紹文
- 附同義詞表，解決使用者用詞與資料用詞對不上的問題
  （吃素→精進料理、賞楓→紅葉、買藥→藥妝、信長→織田信長…）
- 只保留與最高分同量級的結果，避免把勉強沾上邊的景點也塞給導遊
- 純前端、零相依套件，不需要 embedding 或向量資料庫

v15（共享清單 / Firebase）：
- 「已選景點」區新增「☁️ 分享這份清單」與「📂 開啟共享清單」
- 可以把目前勾選的景點存成一份具名清單，其他人打開網頁就能載入同一份
  （例如妹妹存「妹妹想去的」，姊姊在自己手機上按載入就會同步過去）
- 清單可以刪除；同一趟旅程最多保留 30 份
- Firebase SDK 採動態載入，只有真的要用時才從 CDN 下載，不影響開啟速度與離線功能
- 尚未設定 Firebase 時按鈕仍可按，只會說明尚未設定，不會壞掉
- 設定寫在 data.js 的 SHARE_CONFIG，填入 firebaseConfig 即可啟用

v14（真正的離線支援 / PWA）：
- 新增 sw.js（Service Worker）：沒有網路時整個網站仍然打得開
  - 自己的檔案：網路優先、3.5 秒逾時退回快取（規劃時拿最新，現場沒訊號照樣開）
  - 地圖函式庫與圖磚：快取優先（網址有版號，抓過就不再抓，省漫遊流量）
  - 圖磚最多保留 600 張，超過從最舊的開始刪，不會吃光手機空間
  - 導遊的 API 一律不快取
- 新增 manifest.json 與圖示：Android 可以「加到主畫面」像 App 一樣開啟
- 沒有網路時頂端顯示提示，說明哪些功能仍可使用
- 修正：上一版為了擋地圖版權文字而加的 z-index，讓 .map-area 產生堆疊脈絡，
  導致景點介紹／日語小抄／行前準備的面板整個被清單蓋住看不見
  → 改用 display:none 隱藏 MapLibre 控制項，不碰堆疊脈絡

v13（線上導遊的回答品質與連線處理）：
- 導遊現在收到的是景點的完整資料（介紹文、預約規定），不再只有名字
  → 它會依照 data.js 的內容回答，而不是憑自己的記憶，答案比較不會跑掉
- 一併帶入相鄰景點的直線距離，導遊才有依據判斷順不順路
- Render 免費方案休眠時，第一次失敗會自動重試一次，
  並顯示「後端休眠中，正在喚醒…（最多約 60 秒，只有第一次會這麼久）」
- 請求加上逾時（首次 30 秒、重試 70 秒），手機訊號差時不會一直轉圈
- 偵測到手機沒有網路時直接說明，不用等連線失敗
- 後端的內容長度上限一併放寬（2000 → 6000 字），避免景點資料被截斷

v12（Android 實機回報的兩個版面問題）：
- 修正標題「京都行程景點地圖」被三顆按鈕擠成直排 4 行
  → 手機上標題與按鈕各佔一整列，三顆按鈕平分寬度；副標題在手機隱藏
  → 標題列高度 84px → 79px，但標題只剩一行，清單多出可用空間
- 修正地圖的版權文字（MapLibre｜OpenFreeMap）穿透顯示在景點清單中間
  → 原因：MapLibre 的版權控制項自己宣告 visibility:visible，
    會蓋過父層的 visibility:hidden
  → 解法：再用 z-index 讓不透明的側欄實際蓋住它（切到地圖時正常顯示）

v11（日語小抄擴充 44 → 69 句）：
- 新增「💊 藥妝店買藥」：感冒藥、腸胃藥、止痛藥、怎麼吃、長輩能不能吃
- 新增「👴 長輩同行時」：電梯、無障礙通道、階梯多不多、借輪椅、哪裡可以坐
- 新增「⛩️ 參拜與禮儀」：可以拍照嗎、要脫鞋嗎、參拜費、幾點關門、御朱印
- 新增「🏨 飯店」：寄放行李、退房時間、Wi-Fi 密碼、熱水沒熱水、加毛巾
- 全部仍是靜態資料，沒有網路也能開

v10（導遊後端程式碼）：
- 新增 ai-backend/ 資料夾，內含線上導遊的後端（零相依套件，只用 Node 內建模組）
- 新增根目錄 .vercelignore，把 ai-backend/ 排除在 Vercel 之外
  → 根目錄仍然沒有 package.json，Vercel 的靜態部署完全不受影響
- 新增根目錄 render.yaml（Blueprint），rootDir 指向 ai-backend
- 不需要另開 repo：Render 支援指定子目錄部署
- 部署步驟見 ai-backend/README.md

v9（線上導遊）：
- 右上角新增「🧭 線上導遊」，開啟聊天視窗，可詢問景點特色、排行程、現場突發問題
- 導遊看得到你目前勾選的景點與順序，回答會直接對應那些地方
- 附 6 個建議問題快捷鍵；Android 返回鍵會先關閉聊天視窗
- 後端網址寫在 data.js 的 AI_CONFIG.endpoint，留空時按鈕仍可開啟但會說明尚未設定
- 一進網頁就會先敲後端的健康檢查，把 Render 休眠中的服務提早叫醒
- 後端程式碼另放在獨立專案（kyoto-ai），本專案仍是零相依的純靜態網站

v8（日語求助小抄）：
- 右上角新增「💬 日語小抄」按鈕，44 句現場用得到的日文
- 最上方是住宿地址的日文寫法與發音，搭計程車直接拿給司機看
- 緊急電話（110／119／駐大阪辦事處／外交部專線）可直接點擊撥號
- 分類：找地方／身體不舒服／緊急狀況／吃飯購物／租借交通／聽不懂的時候
- 每句都有「中文意思 → 日文原文 → 中文近似發音」三行，日文字級加大方便拿給對方看
- 內容全部寫在 data.js 的 PHRASES，純靜態、不需要網路也能看

v7（事前預約與證件）：
- 景點新增選填的 booking 欄位，分三級：需事前申請 / 必須預約 / 建議預約
- 清單上直接顯示標籤，詳細介紹顯示該做什麼的提醒方塊
- 搜尋框下方新增「只看需要預約／申請的景點」篩選（目前共 16 個）
- 右上角新增「🛂 行前準備」按鈕：護照、免簽、Visit Japan Web、免稅等證件事項
- 行前準備內容放在 data.js 的 TRIP_PREP，之後要改文字改那裡就好

v6：
更新時間：2026-09-21

本版更新：
- 修正 RESI STAY HEART 座標（依官方「市巴士烏丸七条站步行1分鐘」修正到烏丸七条路口一帶）
- 景點新增選填的 address 欄位；有填地址時「在 Google 地圖上看」改用地址查詢，比座標精準
- 飯店介紹補上日文地址，搭計程車可直接出示

v5（響應式版面，以 Android 手機為主要使用情境）：
更新時間：2026-09-21

本版新增內容（響應式版面，以 Android 手機為主要使用情境）：
- 手機版改為「景點清單／地圖／已選」三個畫面，用底部導覽列切換，不再上下擠在一起
- 景點詳細介紹在手機上改為由下往上浮出的面板，可按 ✕ 關閉或直接跳到地圖
- Android 返回鍵會先關閉浮出面板，不會一按就離開網頁
- 分類頁籤在手機上改為橫向滑動；按鈕、核取方塊放大到適合手指點按的尺寸
- 搜尋框字級調為 16px（避免部分手機聚焦時自動放大整個畫面）
- 改用 dvh 計算高度，手機網址列伸縮時版面不會被截斷
- 新增平板（861～1100px）的中間尺寸樣式
- 修正：已選清單改為 flex 版面後不見的 1、2、3 順序編號（改用自訂編號圓點）

v4（行程模擬功能強化）：
- 勾選的景點會在地圖上用虛線連起來，圓點內顯示 1、2、3 順序編號
- 已選清單每一列新增 ▲ ▼ 調整順序、✕ 移除，不用再整批清除重來
- 景點詳細介紹新增「在 Google 地圖上看」連結（照片／評價／營業時間）
- 路線按鈕顯示兩點間直線距離，並加總全程直線距離

v3：
- 新增「住宿與交通」分類（RESI STAY HEART 住宿、關西國際機場、京都車站）
- 景點總數：111 → 114
- README 同步更新為 categories（陣列）寫法與現有 8 個分類

v2：
- 新增3個主題分類：動漫迷陰陽師之旅 / 軍事迷織田信長之旅 / 宇治抹茶體驗之旅
- 景點總數：101 → 111
- data.js 的景點欄位從 category（單一分類）改為 categories（陣列，可多分類）

v1（舊版，已停用）：
- 只有 寺與神社 / 穿搭與藥妝 / 美景 / 精進料理 4個分類，共101個景點
