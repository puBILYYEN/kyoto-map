# 小模型快速卡（Ornith-1.5-9B 等 9B 級模型專用）

你的上下文有限，而且長程式碼容易寫壞。這個專案已經替你準備好「現成指令」，
**照這張卡片做就好，不需要讀其他大檔案。** 這張卡片沒寫到的狀況，請看最後一段。

## 五條鐵律

1. **不要讀整個 `app.js` 或 `data.js`**（各有 6～8 萬字，塞不進你的上下文）。
   要看某個地方：先用 `node tools/find.js 關鍵字` 或 `grep -n "關鍵字" app.js` 找到行號，
   再用 `sed -n '起始行,結束行p' 檔名` 只看那 20 行左右。
2. **能用下面現成指令做的事，一律用指令**，不要自己寫程式去改檔案。
3. 一定要自己改程式時：**一次只改幾行**，絕對不要把整個檔案重新輸出。
4. 改完一定跑 `node tools/check.js`。有 ❌ 就不准 commit；看到「少了超過 20%」就用
   `git checkout -- 檔名` 還原重來。
5. **一律繁體中文、台灣用語**（影片、資訊、軟體、品質、預設、網路、計程車），不要出現簡體字。

## 任務 A：使用者說「網站怪怪的」

1. 請他在網站「已選景點」最下面按「🪵 下載系統記錄」（在電腦上）或
   「📋 複製精簡記錄」（在手機上），貼給你。
2. **只看「===== 診斷摘要 =====」那一段**，裡面已經自動判斷好了。時間已經是當地時間，不用換算。
3. 照下表回答使用者，說明原因＋他要做什麼：

| 摘要裡看到 | 原因 | 怎麼處理 |
|---|---|---|
| ❗ TDZ 錯誤 | app.js 程式順序錯 | 超出這張卡片 → 寫診斷報告，交給 Claude / Gemini |
| ❗ 程式錯誤 | 最近一次更新改壞了 | 同上；報告裡附上錯誤訊息與「新版接管」時間 |
| ❗ Firestore 權限被擋 | 雲端規則沒發布 | 請使用者到 Firebase Console → Firestore → 規則，貼上 README 裡的規則並按「發布」 |
| ⚠️ 家人選點變成 0＋ℹ️ 按過清除選取 | 自己按了清除 | 告訴他這是正常的，重新勾選即可 |
| ⚠️ 家人選點變成 0（沒有清除記錄） | 要看那個家人的手機 | 請那位家人也複製精簡記錄給你 |
| ⚠️ 跳棋／登入載不到、ℹ️ 斷線 | 當時沒網路 | 恢復網路後重新整理；景點清單、日語小抄、行前準備離線也能用 |
| ⚠️ 檔案載入失敗 vendor/ | 地圖函式庫沒載到 | 重新整理；還是不行就換網路 |
| ⚠️ 導遊失敗 E3 / E4 | 後端在睡覺或網路慢 | 等 30 秒再問一次（免費主機閒置會休眠） |
| ✅ 沒有發現已知的故障模式 | 看「最近的錯誤與警告」 | 看不懂就寫診斷報告 |

## 任務 B：查景點

```bash
node tools/find.js            # 列出所有分類與景點數
node tools/find.js 壬生寺      # 用名稱、地區、地址或 id 搜尋，會顯示 id 與行號
```

## 任務 C：改座標（使用者貼 Google 地圖座標）

使用者給的座標**照抄**，不要自己「修正」。格式是「緯度, 經度」（35 開頭在前、135 開頭在後）。

```bash
node tools/set-coords.js t65 35.001872205038815, 135.74336623943248
```

## 任務 D：新增景點

```bash
node tools/add-spot.js --cat temple --name "景點名稱" --area "地區" \
  --lat 35.0 --lng 135.7 --desc "介紹文字" --address "地址" --hours "9:00–17:00"
```

- `--cat` 的值用 `node tools/find.js` 列出來的英文 key，多個分類用逗號：`--cat temple,enmusubi`
- `--address`、`--hours` 可以不寫。**營業時間不確定就不要寫**，寫錯比沒寫更糟
- 座標是自己估的（不是使用者給的），`--desc` 開頭加「⚠️ 座標為依地址估算，尚未用Google地圖核對。」
- id 會自動產生，不用自己編

## 任務 E：新增分類

```bash
node tools/add-category.js onsen "溫泉之旅"     # key 用小寫英文，顏色自動挑
```

會同時改好 `data.js` 和 `app.js` 兩個地方。接著用任務 D 加景點（`--cat onsen`）。

## 任務 F：上線（每次改完都要做）

```bash
node tools/bump.js "這次改了什麼（一句話）"   # 自動把版號加 1、寫更新紀錄
node tools/check.js                           # 必須看到 ✅ 才能繼續
git status                                    # 確認只改了預期的檔案
git add data.js app.js sw.js VERSION.txt      # 只加 git status 裡列出、你改過的檔案
git commit -m "這次改了什麼"
git push origin 目前的分支
```

推到 `main` 分支後約 1 分鐘網站就會更新。請使用者**完全關掉網頁再重新打開**才看得到新版。
不確定要不要直接推到 `main`，先問使用者。

## 卡片沒寫到的狀況

以下這些**不要自己改**，改錯會讓全家人資料不同步或網站打不開：
Google 登入、跳棋同步、Firestore 規則、`sw.js` 的快取邏輯、`app.js` 裡任何超過 10 行的修改。

請改寫一份「診斷報告」給使用者，讓他轉交給 Claude 或 Gemini：

```
【診斷報告】
症狀：（使用者說了什麼）
發生時間：（記錄裡的時間）
網站版本：（記錄開頭的「網站版本」）
診斷摘要重點：（貼上自動判斷那幾行）
我的猜測：（一兩句，不確定就寫不確定）
需要接手的 AI 做：（例如「修 app.js 的 TDZ 錯誤」）
```

## 如果你是在手機上跑（不能讀檔案、不能執行指令）

你只能做「任務 A」：看精簡記錄、說明原因、告訴使用者該怎麼做或交給誰修。
**不要叫使用者自己去改程式碼**，他是寫程式的新手，照著貼很容易把網站改壞。
